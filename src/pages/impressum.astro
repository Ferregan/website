---
import { getEntry } from "astro:content";
import BaseLayout from "../layouts/BaseLayout.astro";

const impressum = await getEntry("about", "impressum");
const { Content } = await impressum?.render();

---

<BaseLayout>
  <Content />
</BaseLayout>
